// Flutter properties for all subprojects
subprojects {
    project.ext {
        flutter = [
            compileSdkVersion: 35,
            minSdkVersion: 23,
            targetSdkVersion: 35,
            versionCode: 1,
            versionName: "1.0.0"
        ]
    }
}

